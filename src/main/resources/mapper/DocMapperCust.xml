<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!--  这里的地址是interface的地址： mapper.DocMapperCust-->
<mapper namespace="com.zoe.wiki.mapper.DocMapperCust" >

  <!--浏览数+1-->
  <select id="increaseViewCount" >
    update doc set view_count = view_count + 1 where id = #{id}
  </select>

  <!--点赞数+1-->
  <select id="increaseVoteCount" >
    update doc set vote_count = vote_count + 1 where id = #{id}
  </select>

  <!--更新ebook-->
  <select id="updateEbookInfo" >
    update ebook t1, (select ebook_id, count(id) doc_count, sum(view_count) view_count, sum(vote_count) vote_count from doc group by ebook_id) t2
    set t1.doc_count = t2.doc_count, t1.view_count = t2.view_count, t1.vote_count = t2.vote_count
    where t1.id = t2.ebook_id
  </select>

</mapper>
